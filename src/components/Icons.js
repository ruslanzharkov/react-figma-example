import React, { Component } from 'react';

class Icons extends Component {
    render() {
        return(
            <div>
                Здесь должны были быть иконки, но кажется их нет(
            </div>
        );
    }
}

export default Icons;